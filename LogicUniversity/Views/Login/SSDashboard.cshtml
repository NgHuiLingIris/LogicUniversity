
@{
    ViewBag.Title = "Store Manager Dashboard";
}
@{
    string sessionId = (string)ViewData["sessionId"];
}
@{
    Layout = "~/Views/Shared/_Layout_SS.cshtml";
}


<head>
    @*<link rel="stylesheet" type="text/css" href="~/css/newst.css">*@

</head>


<h2>Store Supervisor Dashboard</h2>
<a href="@Url.Action("ViewRequisition","RequisitionApprovals",new { sessionId = sessionId })" class="button">Requistion Approvals</a>

<a href="@Url.Action( "PlaceOrder", "PurchaseOrder", new { sessionId = sessionId }, null)" class="button">PurchaseOrder</a>
        <td>@Html.ActionLink("Catalogue", "Index", "Products", new { sessionId = sessionId }, null)</td>
        <td>@Html.ActionLink("Pending Approval", "ApproverView", "StockAdjustmentVoucherDetails", new { Role = "Supervisor", sessionId = sessionId }, null)</td>

    </tr>
    <tr>
        <td>@Html.ActionLink("Manage Collection Point", "ViewCollectionPoint", "CollectionPoint", new { sessionId = sessionId }, null)</td>
        <td>@Html.ActionLink("Supplier List", "Index", "Suppliers", new { sessionId = sessionId }, null)</td>
        <td>@Html.ActionLink("View Tender", "Tender")</td>
    </tr>
    <tr>
        <td>@Html.ActionLink("Requisitions History", "ViewAllRequisition", "RequisitionApprovals", new { sessionId = sessionId }, null)</td>
        <td>@Html.ActionLink("View Department", "Department")</td>
        <td>@Html.ActionLink("Charge Department", "ChargeBackDepartment", "ChargeBackDepartment", new { sessionId = sessionId }, null)</td>
    </tr>
    <tr>
        <td>@Html.ActionLink("Trend Analysis", "Index", "Trend", new { sessionId = sessionId }, null)</td>
        <td>@Html.ActionLink("Reorder Report", "Department", "")</td>
        <td>@Html.ActionLink("Inventory Status Report", "ChargeBackDepartment", "ChargeBackDepartment", new { sessionId = sessionId }, null)</td>
        <td></td>
    </tr>

</table>

    <style>
        fieldset {
            display: block;
            margin-left: 2px;
            margin-right: 2px;
            padding-top: 0.35em;
            padding-bottom: 0.625em;
            padding-left: 0.75em;
            padding-right: 0.75em;
            border: 2px groove;
        }

        .button {
            background-color: #0000ff; /* Green */
            border: none;
            color: white;
            width: 300px;
            height: 100px;
            padding: 40px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 15px 20px;
            cursor: pointer;
            border-radius: 2px;
            position: relative;
        }
    </style>